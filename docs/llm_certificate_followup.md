# Fixed-pair certificate follow-up preregistration

Registered 2026-09-21 for issue [#9](https://github.com/swear01/AIsimpV/issues/9).
This is a separate run after the original infrastructure-blocked pilot, authorized
as part of the complete Wednesday delivery. The original four attempts, raw ledger
and 309.73984032624867 charged seconds remain unchanged. This new run does not
turn the original pilot into a success or replace the joint-generation experiment.

## Frozen task and budget

- Run ID: `skid8-certificate-inline-20260921`.
- Source: merged commit `9dd8e24fbc60057f3ca5100f603f0738f655b8d6`.
- Task: fixed R2-skid8 concrete RTL and manually prepared occupancy abstraction;
  generate only the typed `h/J/w` certificate.
- Concrete canonical hash: `397147cf737063964958eb252f95bcd850c0e0390377fd6b027f4b787a10da96`.
- Abstract canonical hash: `66894d5fdffc526f302d9bf8fbc0e610e5ccfc24845b64c9f0934d304f671fd6`.
- Contract canonical hash: `71d949ec52f2b6b8e63c30d52e883ee25f381a2724bfd38f35e5b86db5f75ac2`.
- Original certificate ledger file hash: `d2ae2e6c765a3a64bc09c33e1dc4d6c9667524271a09cf563c9df6b112df50dc`.
- At most four candidates, including syntax/type errors, certificate rejections,
  solver failures and timeouts. At most 900 seconds total charged generation and
  verification time; existing per-stage caps still apply. Stop on success or
  exhaustion. An infrastructure error stops the run for diagnosis.
- Use the existing configured OpenAI Codex model and reasoning effort, recording
  the actual configuration and CLI version. No model selection based on results.

## Qualification and isolation

First qualify the current inline bundle and structured-string output transport
with a trivial request containing no RTL, certificate or relationship hints.
Record that model call, usage, time, command and OS boundary probe separately as
engineering qualification; it is neither free nor a research candidate.

Prepare the fixed-pair bundle with the existing runner. It may contain only the
actual C/A RTL, C/A transition models and symbol manifests, frozen contract, and
generic typed-expression syntax. The coordinator audits the complete bytes and
freezes bundle, prompt and trusted-source hashes before any research generation.
The generator receives no gold certificate, plan formulas, Git metadata, agent
history or other research results. Each attempt is a fresh ephemeral CLI context;
only its own prior raw candidate and actual formal feedback enter a repair.

The parent stores returned strings verbatim. It does not repair expression types,
formulas or hashes. All `h/J/w` obligations use the independent checker. Abstract
property checking keeps nondeterminism free; witness substitution stays in the
certificate harness. C, A and the contract never change within this run.

## Outcomes and accounting

Report every attempted candidate and its generation, parsing/type, gate and
property outcomes. A successful fixed-pair discovery requires an accepted
certificate; the independently proved abstract property is reported separately.
Keep exact prompts, raw outputs, candidate hashes, queries, solver outputs and
per-stage costs. Record unavailable token/currency costs as unavailable.

Recheck saved candidates from a clean trusted source without another model call
or candidate edits, recording recheck cost separately. Compare classifications
and immutable task/candidate hashes. The report must distinguish the original
infrastructure run, this follow-up, and joint RTL generation, and include all
their costs. Do not combine their differing tasks into a success rate.

This single fixed pair and feedback chain can show one concrete relation-discovery
outcome. It cannot establish general LLM success rates, a causal benefit of formal
feedback, or verification acceleration. Method failures are valid observations;
transport failures alone do not answer the relation-discovery question.
